import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['fakestoreapi.com'], // Allow images from the 'fakestoreapi.com' domain for the Image property key

  },
};

export default nextConfig;
